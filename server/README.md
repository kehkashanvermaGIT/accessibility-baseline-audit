# Server

The `server/` directory contains the backend of the application.

## Responsibilities

- API endpoints
- Application logic
- Data processing
- Server-side validation
- Communication with data storage when required